<form:label path="${fieldName}" cssClass="control-label col-xs-12 col-sm-4">${fieldTitle}</form:label>
<div class="col-xs-12 col-sm-8">
#foreach(${code} in ${codes})
  <label class="checkbox-inline">
    <form:checkbox path="${fieldName}" value="${code.code}">${code.value}</form:checkbox>
  </label>
#end
</div>
